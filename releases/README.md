# Release files

- Put the tested Windows EXE installer in `windows/` when preparing a manual upload.
- Linux AppImage, DEB and RPM files are generated automatically by GitHub Actions.
- For a combined automatic release, push a version tag such as `v0.4.0`.
